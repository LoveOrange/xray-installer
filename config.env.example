# Xray Installer Configuration
# Copy this file to config.env and modify as needed
# Usage: sudo bash install.sh --config config.env

#===============================================================================
# User Configuration
#===============================================================================
XRAY_USER="xray"
# XRAY_HOME is automatically set to /home/${XRAY_USER}

#===============================================================================
# Domain and Email
#===============================================================================
DOMAIN="sub.example.com"
EMAIL="admin@example.com"

#===============================================================================
# Security Mode: "tls" or "reality"
#===============================================================================
SECURITY_MODE="tls"

#===============================================================================
# REALITY Settings (only used if SECURITY_MODE="reality")
#===============================================================================
# Target website to mimic (must support TLS 1.3 and HTTP/2)
REALITY_DEST="www.microsoft.com:443"

# Server names (comma-separated, from target's certificate)
REALITY_SERVER_NAMES="www.microsoft.com,microsoft.com"

#===============================================================================
# Camouflage Website
# Options: "hackernews", "custom", "static"
#===============================================================================
CAMOUFLAGE_SITE="hackernews"

# Custom URL (only used if CAMOUFLAGE_SITE="custom")
CAMOUFLAGE_URL="https://news.ycombinator.com"

#===============================================================================
# Secondary IP Configuration (Optional)
# Use Cloudflare WARP or another proxy for specific sites
#===============================================================================
USE_SECONDARY_IP="no"

# If using a remote server instead of WARP
SECONDARY_IP=""
SECONDARY_USER=""
# Note: Password should be entered interactively for security

#===============================================================================
# Directory Configuration (Optional - defaults are usually fine)
#===============================================================================
# CERTS_DIR="${XRAY_HOME}/certs"
# XRAY_DIR="${XRAY_HOME}/xray"
# WEB_DIR="${XRAY_HOME}/web"
# LOG_DIR="${XRAY_DIR}/logs"
